# Teacher Answers — Encapsulation

This file is outside `docs` and is not included in the pupil site.

1. The public property is `username`; the private property is `__score`; the accessor is `get_score()`; and the mutator is `set_score()`.
2. Making the score private prevents external code from bypassing the validation in `set_score()`.
3. The method checks that `250` is at least zero and stores it in `self.__score`.
4. The output is `250`.
5. `player1.set_score(-10)` displays `Score cannot be negative.` and leaves the stored score as `250`.

